physical_order:
    user: user
    number: <%= "R#{Array.new(9){rand(9)}.join}" %>
    state: paid
    item_total: 0.99
    created_at: <%= Time.now.to_s(:db)%>
    completed_at: <%= Time.now.to_s(:db)%>
    total: 0.99
    adjustment_total: 0.20
